# Security

Do not open public issues for leaked tokens.

- `cursor-crew doctor` never prints the Cursor access token.
- Do not commit `.env`, `state.vscdb`, or `CURSOR_AUTH_TOKEN`.
- Report privately to the repository owner.

Email for this project: use GitHub private vulnerability reporting on the repo when enabled.
